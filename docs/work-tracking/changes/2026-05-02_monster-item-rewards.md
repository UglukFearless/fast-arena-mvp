# Награды предметами с монстров

## Предложения

## Ссылки на предметную область

- [`docs/domain/combat.md`](../../domain/combat.md)
- [`docs/domain/items.md`](../../domain/items.md)

## Объём

- Применяется только к победе в действиях активности `MONSTER_FIGHT`.

## Критерии приёмки

- Генерация награды за бой с монстром сохраняет существующий расчёт золота.
- Генерация награды за бой с монстром может добавлять дополнительные награды предметами, настроенные для побеждённого типа монстра.
- Запись награды предметом — один независимый бросок дропа; для одного типа монстра допускаются несколько записей с одним и тем же предметом.
- Выпавшие стекуемые предметы группируются по идентичности предмета с суммированием количества; нестекуемые остаются отдельными экземплярами. Группировка действует и при выдаче в инвентарь героя, и в панели наград UI.
- DTO награды, возвращаемый эндпоинтами боя с монстром, включает все выпавшие предметы.
- Начальный рантайм-seed определяет стартовую таблицу наград для существующих типов монстров.
- UI боя показывает полученные предметы-награды на экране боя, включая изображение предмета.

## Архитектурный контракт

- Хранить записи наград предметами с монстров в DAL как связь от `MonsterMold` к `Item` с полями `ChancePercent` и `Amount`.
- Количество одного предмета задаётся несколькими независимыми записями без диапазона количества: так гарантированный дроп и редкие дополнительные копии выражаются без отдельных правил.
- Загружать записи наград вместе с данными типа монстра, необходимыми для генерации наград.
- Держать генерацию наград в `MonsterFightService`, чтобы публичный контракт боя оставался неизменным.
- Переиспользовать существующий поток `MonsterFightReward -> GivenItem -> GiveItemsToHeroAsync`.

## План

### Backend

- [x] Добавить поддержку DAL/сущности и маппинга для записей наград с монстров.
- [x] Расширить загрузку типа монстра, включив записи наград и связанные предметы.
- [x] Обновить генерацию награды за бой с монстром, добавив брошенные награды предметами.
- [x] Засеять начальные записи наград для существующих типов монстров.
- [x] Добавить точечные тесты на генерацию наград и поток сохранения наград.

### Frontend

- [x] Отображать предметы наград как структурированные записи вместо простой HTML-строки.
- [x] Показывать в существующей панели наград боя изображение, название и количество каждого полученного предмета-награды.
