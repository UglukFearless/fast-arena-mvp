# Фича: Использование оружия и щита в бою

## Предложения

## Цель

Дать оружию и щиту влияние на бой через общий пайплайн эффектов, включая постоянные эффекты экипировки в течение боя.

## Ссылки на предметную область

- [`docs/domain/items.md`](../../domain/items.md)
- [`docs/domain/combat.md`](../../domain/combat.md)
- [`docs/domain/effects.md`](../../domain/effects.md)

## Критерии приёмки

1. Экипированное оружие изменяет исходящую силу удара и/или итоговый урон согласно определению эффекта предмета.
2. Экипированный щит может полностью заблокировать входящий удар с заданным шансом и расходует ограниченное число успешных блоков.
3. Эффекты экипировки и эффекты расходуемых предметов действуют в одном бою одновременно.
4. Ответ боя отражает эффективный исход раунда после применения эффектов экипировки и расходуемых предметов.

## Архитектурные заметки

- Расширить рантайм-модель активного эффекта полями:
	- `LifetimeType` — `RoundBased` или `Persistent`.
	- `SourceType` — `Potion`, `Equipment` или `Skill`.
- Постоянные эффекты инициализируются в начале боя из экипированных предметов и не подлежат ни пораундовому отсчёту, ни объединению стека.
- Применимость эффекта определяет его типизированный обработчик; общее поле условия `ConditionType` удалено.

## План

### Backend

- [x] Удалить `ConditionType` из `EffectDefinition`, `ActiveEffect`, enum и seed-данных.
- [x] Добавить `LifetimeType` и `SourceType` в `ActiveEffect`.
- [x] Добавить новые типы эффектов экипировки: `STRIKE_POWER_BONUS`, `UNIT_DAMAGE_DELTA`, `INCOMING_STRIKE_FULL_BLOCK`.
- [x] Инициализировать постоянные эффекты экипировки из экипированного оружия и щита в начале боя.
- [x] Подключить `UNIT_DAMAGE_DELTA` в Фазу E (до фиксации урона) жизненного цикла боя.
- [x] Подключить `INCOMING_STRIKE_FULL_BLOCK` в Фазу C (подтверждение входящего удара) жизненного цикла боя.
- [x] Отфильтровать эффекты с `SourceType = Equipment` из ответа DTO раунда боя.
- [x] Привязать seed-предметы оружия и щита к определениям эффектов.
- [x] Добавить backend-тесты на применение эффектов оружия и щита и поведение экипировки/снятия.
- [x] Расширить каталог магазина и валидацию покупки, включив `WEAPON` и `SHIELD`.
- [x] Включить экипировку/снятие оружия и щитов через `HeroEquipmentService` с проверками конфликта двуручности.

### Frontend

- [x] Разрешить экипировку/снятие оружия и щитов из UI экипировки героя.
- [x] Показывать результат блока щитом в UI раунда боя.
- [x] Добавить выделенный компонент панели экипировки в окне боя и привязать его к экипированным слотам героя.
- [x] Вынести панель активных эффектов как отдельный компонент в окне боя.
- [x] Перенести источник истины предметов кармана на экипированные слоты героя; убрать предметы кармана из DTO состояния боя.

## Отклонённый вариант

- Отдельный пайплайн боевых модификаторов оружия и щита вне системы эффектов: дублировал бы жизненный цикл и порядок применения, уже реализованные в пайплайне эффектов.
